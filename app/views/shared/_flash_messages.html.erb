<% if flash.any? %>
    <% flash.each do |type, msg| %>
        <div class="fixed-bottom w-25 me-3 ms-auto alert alert-<%= type %> alert-dismissible fade show" role="alert">
            <%= msg %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    <% end %>
<% end %>